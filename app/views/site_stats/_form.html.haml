= form_for @site_stat do |f|
  - if @site_stat.errors.any?
    #error_explanation
      %h2= "#{pluralize(@site_stat.errors.count, "error")} prohibited this site_stat from being saved:"
      %ul
        - @site_stat.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :url
    = f.text_field :url
  .actions
    = f.submit 'Save'
